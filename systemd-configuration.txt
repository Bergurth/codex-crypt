so this should be incorporated into the setup file
--> so as to bring about this situation .. or one like it
--->
also: sudo systemctl daemon-reload
and then: sudo sytemctl enable clear-crypt.service

 etc ❯ systemd ❯ system 
❯_: cat clear-crypt.service 
[Unit]
Description=Pre-Shutdown Processes
DefaultDependencies=no
After=final.target
# This works because it is installed in the target and will be
#   executed before the target state is entered
# Also consider kexec.target

[Service]
Type=oneshot
ExecStart=/usr/bin/bash /home/bergurth/scripts/pre-shutdown.sh  # your path and filename

[Install]
WantedBy=final.target    

--------------------------------------------------------------------------------

❯_: cat pre-shutdown.sh 
rm -rf /home/bergurth/projects/codex-crypt/secrets/*
exit 0

